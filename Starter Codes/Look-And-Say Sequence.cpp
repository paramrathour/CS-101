// Author: param3435
#include <simplecpp>

const int LENGTH_MAX = 1e5;

main_program {
	int LSS[LENGTH_MAX], length = 0;
	int n;
	cin >> n;
	repeat (n) {
		// calculate sequence at each iteration from the sequence at previous iteration
		cout << "\n";
	}
	return 0;
}