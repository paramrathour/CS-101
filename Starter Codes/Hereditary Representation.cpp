// Author: param3435
#include <simplcpp>

void Hereditary(long long num, int base);

main_program {
	int total_test_cases;
	cin >> total_test_cases;
	for (int t = 0; t < total_test_cases; t++) {
		long long num;
		int base;
		cin >> num >> base;
		Hereditary(num, base);
		cout << "\n";
	}
	return 0;
}

void Hereditary(long long num, int base) {
	// print the hereditary representation here
	return;
}