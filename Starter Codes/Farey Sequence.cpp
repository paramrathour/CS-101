// Author: param3435
#include <simplecpp>

void draw_circle(int x_offset, int y_offset, int scale, int x, int y);
void farey(int a, int b, int c, int d, int level, int n, bool init = true);

main_program {
	int n;
	cin >> n;
	initCanvas("Ford Circles", 1920, 1080);
	// generate and draw the farey sequence
	getClick();
	return 0;
}