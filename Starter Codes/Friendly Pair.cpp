// Author: param3435
#include <simplecpp>

long long sum_of_divisors(int n);
bool friendly_pair_check(int a, int b);

main_program {
	int total_test_cases;
	cin >> total_test_cases;
	repeat (total_test_cases) {
		int a, b;
		cin >> a >> b;
		friendly_pair_check(a, b);
		cout << "\n";
	}
	return 0;
}

long long sum_of_divisors(int n) {
	long long sum = 0;
	// return the sum of divisors of n
	return sum;
}

bool friendly_pair_check(int a, int b) {
	// check if a,b are friendly and output the common abundancy index or -1 accordingly
}