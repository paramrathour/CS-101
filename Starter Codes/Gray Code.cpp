#include<simplecpp>

const int MAX_n = 10;
const int MAX_size = 1024;

main_program {
	int n, power = 2;
	cin >> n;
	int size = 1;
	repeat(n) {
		size *= 2;
	}
	int digits[MAX_size][MAX_n];
	// generate gray code here
}
