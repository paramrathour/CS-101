#include <simplecpp>

void regular_star_polygon(int n, int m);

main_program {
	int n, m, iterations_outer, iterations_inner, gcdnm;
	cin >> n >> m;
	turtleSim();
	regular_star_polygon(n, m);
	wait(10);
}

void regular_star_polygon(int n, int m) {
	// draw the correspoding regular star polygon here.
}