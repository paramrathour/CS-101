#include<simplecpp>

const int MAX_n = 100;

main_program {
	int total_test_cases;
	cin >> total_test_cases;
	int A[MAX_n][MAX_n];
	for (int i = 0; i < MAX_n; ++i) {
		for (int j = 0; j < MAX_n; ++j) {
			A[i][j] = 0;
		}
	}
	repeat(total_test_cases) {
		int n, current = 1;
		cin >> n;
		// generate the spiral grid and print it appropriately
		cout << endl;
	}
}
