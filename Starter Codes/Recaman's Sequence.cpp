#include<simplecpp>

const int MAX_n = 100000;

main_program {
	int n, num;
	cin >> n;
	int r[MAX_n + 1] = {0};
	// calculate the sequence here

	int w = 1920, h = 1080;
	initCanvas("Recaman Sequence", w, h);
	Turtle t1;
	int sides = 100;
	// draw the curve here (you can also choose to draw the curve simulataneously with the sequence elements)
	getClick();
}